//WAP to find even and odd by calling the user define function
//through if statements 

#include<stdio.h>
void main(int n)
{
    return n;
}
int main()
{
    int n;
    scanf("%d",&n);
    if(n%2==0)
    {
        printf("the number is even\n :%d",n);
    }
    if(n%2!=0)
    {
        printf("the number is odd");
    }
    
}
